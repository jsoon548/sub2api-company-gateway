// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/Wei-Shaw/sub2api/ent/superadminseat"
	"github.com/Wei-Shaw/sub2api/ent/user"
)

// SuperAdminSeat is the model entity for the SuperAdminSeat schema.
type SuperAdminSeat struct {
	config `json:"-"`
	// ID of the ent.
	ID int16 `json:"id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID int64 `json:"user_id,omitempty"`
	// Version holds the value of the "version" field.
	Version int64 `json:"version,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SuperAdminSeatQuery when eager-loading is set.
	Edges        SuperAdminSeatEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SuperAdminSeatEdges holds the relations/edges for other nodes in the graph.
type SuperAdminSeatEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SuperAdminSeatEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SuperAdminSeat) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case superadminseat.FieldID, superadminseat.FieldUserID, superadminseat.FieldVersion:
			values[i] = new(sql.NullInt64)
		case superadminseat.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SuperAdminSeat fields.
func (_m *SuperAdminSeat) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case superadminseat.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int16(value.Int64)
		case superadminseat.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				_m.UserID = value.Int64
			}
		case superadminseat.FieldVersion:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field version", values[i])
			} else if value.Valid {
				_m.Version = value.Int64
			}
		case superadminseat.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SuperAdminSeat.
// This includes values selected through modifiers, order, etc.
func (_m *SuperAdminSeat) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the SuperAdminSeat entity.
func (_m *SuperAdminSeat) QueryUser() *UserQuery {
	return NewSuperAdminSeatClient(_m.config).QueryUser(_m)
}

// Update returns a builder for updating this SuperAdminSeat.
// Note that you need to call SuperAdminSeat.Unwrap() before calling this method if this SuperAdminSeat
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *SuperAdminSeat) Update() *SuperAdminSeatUpdateOne {
	return NewSuperAdminSeatClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the SuperAdminSeat entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *SuperAdminSeat) Unwrap() *SuperAdminSeat {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: SuperAdminSeat is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *SuperAdminSeat) String() string {
	var builder strings.Builder
	builder.WriteString("SuperAdminSeat(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.UserID))
	builder.WriteString(", ")
	builder.WriteString("version=")
	builder.WriteString(fmt.Sprintf("%v", _m.Version))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SuperAdminSeats is a parsable slice of SuperAdminSeat.
type SuperAdminSeats []*SuperAdminSeat
